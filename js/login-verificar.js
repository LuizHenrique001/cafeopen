let nome = document.getElementById('usuario').value
let gmail = document.querySelector('input#gmail').value
let senha = document.querySelector('input#senha').value

function verificarLogin() {
    if(nome != ''){
        alert('Logado com sucesso')
    }
}
